ingress:
  enabled: true

nextcloud:
  host: files.thecullensnz.com

image:
  tag: 31.0.1-fpm-alpine

nginx:
  enabled: true

postgresql:
  enabled: true
  global:
    postgresql:
      auth:
        password: "testing123456"

internalDatabase:
  enabled: false

externalDatabase:
  enabled: true
  type: postgresql
  password: "testing123456"

redis:
  enable: true
  auth:
    enabled: false
