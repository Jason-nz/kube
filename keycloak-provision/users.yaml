apiVersion: user.keycloak.crossplane.io/v1alpha1
kind: User
metadata:
  name: john
  namespace: crossplane
spec:
  forProvider:
    enabled: true
    email: john@domain.com
    firstName: John
    lastName: Johnson
    username: john
    realmIdRef:
      name: home
  providerConfigRef:
    name: home
